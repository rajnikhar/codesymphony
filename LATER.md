# Later (not v1)

- Redis / multi-instance cache
- Sonification (Web Audio)
- Camera fly-throughs / particle WebGL LOD
- Call graphs
- C/C++ `#include` parsing
- Kotlin / Go / Rust / etc.
- Multi-user WebSocket rooms
- Live clone progress over WebSocket
- Clustering polish beyond dir collapse + top-N
